name: firstlook
on:
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:
    inputs:
      pr-number:
        description: 'PR number to scan (leave empty for all open PRs)'
        required: false
        type: string

jobs:
  assess:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: getagentseal/firstlook@e88f257908a23e857ad717b6bb35a6829cd2b120 # main 2026-09-14
        with:
          pr-number: ${{ inputs.pr-number }}
          skip-users: 'dependabot[bot],renovate[bot]'
          fail-on: 'unknown'
